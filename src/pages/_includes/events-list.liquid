<nav>
  <ul>
    {%- for event in events %}
      <li>
        <a href="{{ event.url }}"{% if currentPage == event.url %} aria-current="page"{% endif %}>
          {{ event.data.title }}
        </a>
      </li>
    {%- endfor %}
  </ul>
</nav>
